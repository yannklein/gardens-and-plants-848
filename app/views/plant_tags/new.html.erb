<%# .container>.row.>.col-6 %>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
    <h1>Add a Tag to your <%= @plant.name %> 🪴</h1>
    <%= simple_form_for [@plant, @plant_tag] do |f| %>
      <%= f.input :tag, collection: Tag.all  %>
      <%= f.submit "Add it!", class: "btn btn-primary" %>
    <% end %>
    
    </div>
  </div>
</div>